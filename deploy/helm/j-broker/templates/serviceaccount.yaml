{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jbroker.serviceAccountName" . }}
  labels:
    {{- include "jbroker.labels" . | nindent 4 }}
{{- end }}
